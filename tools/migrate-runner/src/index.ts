#!/usr/bin/env node
/**
 * 切片 01 — migrate-runner 占位
 * 完整 MySQL 迁移执行器(13 表 DDL + up/down + 索引验证)由切片 03(infra-mysql-ddl)落地。
 */
console.error('[migrate-runner] placeholder; see slice 03 (infra-mysql-ddl)');
process.exit(0);
